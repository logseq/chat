<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!-- Debug builds support user-configured HTTP backends on the local LAN. -->
    <base-config cleartextTrafficPermitted="true" />
</network-security-config>
