# Skip configuration for LogseqChat module
#
# Kotlin dependencies and Gradle build options for this module can be configured here
#build:
#  contents:
#    - block: 'dependencies'
#      contents:
#        - 'implementation("androidx.compose.runtime:runtime")'

# this is a transpiled Skip Lite module
skip:
  mode: 'transpiled'

build:
  contents:
    - block: 'dependencies'
      contents:
        - 'api("com.amplifyframework.ui:authenticator:1.9.2")'
        - 'implementation("com.github.gregcockroft:AndroidMath:v1.1.0")'
        - 'implementation("dev.hossain:compose-highlight:0.27.0")'
        - 'implementation("com.google.mlkit:genai-speech-recognition:1.0.0-alpha1")'

settings:
  contents:
    - block: 'dependencyResolutionManagement'
      contents:
        - block: 'repositories'
          contents:
            - 'maven("https://jitpack.io")'
